const { runCommand } = require("./utils/command");
const package = require("../package.json");
console.log("package: ", package.version);

// runCommand("git status", "../");
// runCommand("git add .", "../");
